-- По итогам сверки архитектуры с пользователем:
-- 1. Встроенный чат (messages) не нужен в MVP — после подтверждения
--    сделки стороны просто видят контакт друг друга.
-- 2. Контакт (telegram/телефон) заполняется участником в своём профиле
--    (экран профиля будет отдельной задачей) — здесь только колонка.

drop table if exists delivery.messages;

alter table delivery.profiles add column contact text;
